// Location for custom route code
function localRouteMethods(route) {
    switch(route) {
        default:
        break;
    }
}

// Background video ended callback
function backgroundVideoOnEndedCallback(routeId) {
    switch(routeId) {
        default:
        break;
    }
}

// Video ended callback
function videoOnEndedCallback(videoId) {
    switch(videoId) {
        default:
        break;
    }
}

// Video before close callback
function videoBeforeCloseCallback(videoId) {
    switch(videoId) {
        default:
        break;
    }
}

// Modal close callback
function onModalClose(modalId) {
    switch(modalId) {
        default:
        break;
    }
}